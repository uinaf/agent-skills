interface:
  display_name: "uinaf Radar"
  short_description: "Read uinaf work and attention state"
  default_prompt: "Use $uinaf-radar to read the private uinaf Radar and summarize Now, Blocked, Next, Inbox, Maintenance, recently Shipped, and Project drift without changing GitHub. Label coverage not audited unless an exact repository list and start date were supplied."
